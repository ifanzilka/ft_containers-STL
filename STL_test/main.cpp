#include <iostream>
#include <vector>

int main()
{
    // Create a vector containing integers
    std::vector<int> v;
 
    std::vector<int>::pointer i;
    // Add two more integers to vector
    v.push_back(25);
    v.push_back(13);
 

    return (0);
}